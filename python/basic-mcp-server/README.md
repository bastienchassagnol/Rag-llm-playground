# Minimal MCP server (course lab)

See the book chapter `python/basic-mcp-server.qmd`.

From the **repository root**:

```bash
uv sync
uv run python python/basic-mcp-server/server.py
```

Wire the command into your MCP host (Cursor, Claude Desktop, or
similar) as a stdio server.
